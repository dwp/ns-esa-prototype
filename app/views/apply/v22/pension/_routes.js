const express = require('express');
const router = express.Router();
//const pensionTracker = require('pension-tracker');
const BASE_PATH = 'apply/v22/pension';
const ABS_BASE_PATH = `/${BASE_PATH}`;











module.exports = router;